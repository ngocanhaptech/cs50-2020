name: Tests lecture 7 airline

on: push

jobs:
  test_project:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run Django unit tests
      run: |
        pip3 install --user django
        cd lecture7/airline/
        python3 manage.py test
